<td class="svg">
    @if ($grade == 'SK')
      <img src="{{ asset('check-mark.svg') }}">
    @endif
  </td>

  <td class="svg">
    @if ($grade == 'K')
      <img src="{{ asset('check-mark.svg') }}">
    @endif
  </td>

  <td class="svg">
    @if ($grade == 'S')
      <img src="{{ asset('check-mark.svg') }}">
    @endif
  </td>

  <td class="svg">
    @if ($grade == 'B')
      <img src="{{ asset('check-mark.svg') }}">
    @endif
  </td>

  <td class="svg">
    @if ($grade == 'SB')
      <img src="{{ asset('check-mark.svg') }}">
    @endif
  </td>
